 -- Socket interface code directory --

 * This is the directory containing the code that deals with the socket 
   interface.

 * Files:
   - interface.py: Deals with the driver communication and the wrapper socket 
      and threading interface.
   - sockets.c: Deals with the wrapper communication and the driver socket 
      interface .
